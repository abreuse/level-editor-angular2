export class Resource{
  name: string;
  code: string;
  x: number;
  z: number;

  constructor(name:string, code:string, x:number, z:number){
    this.name = name;
    this.code = code;
    this.x = x;
    this.z = z;
  }
}
